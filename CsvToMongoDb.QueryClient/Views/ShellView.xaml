<Window x:Class="CsvToMongoDb.QueryClient.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModels="clr-namespace:CsvToMongoDb.QueryClient.ViewModels"
        mc:Ignorable="d"
        Background="{StaticResource WindowBackground}"
        Title="ShellView"
        Height="450"
        Width="800">
    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="Foreground"
                        Value="White">
                </Setter>
            </Style>
            <Style TargetType="ListView">
                <Setter Property="Background"
                        Value="{StaticResource WindowBackground}" />
                <Setter Property="BorderBrush"
                        Value="#36393B">
                </Setter>
            </Style>
            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="White"></Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <DockPanel>
            <Label Content="MachineID:"
                   DockPanel.Dock="Top">
            </Label>
            <ComboBox DockPanel.Dock="Top"
                      ItemsSource="{Binding MachineIds}"
                      SelectedItem="{Binding SelectedMachineId}">
            </ComboBox>
            <Label Content="Parameters:"
                   DockPanel.Dock="Top">
            </Label>
            <Button Content="Search" DockPanel.Dock="Bottom"></Button>
            <ListView ItemsSource="{Binding Parameters}"
                      DockPanel.Dock="Top">
                <ListView.Resources>
                    <DataTemplate DataType="{x:Type viewModels:ParameterViewModel}">
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected, Mode=OneWayToSource}"></CheckBox>
                    </DataTemplate>
                </ListView.Resources>
            </ListView>
        </DockPanel>
        <DataGrid Grid.Column="1" Background="{StaticResource WindowBackground}"
                  ItemsSource="{Binding Results}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="*"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>