<Window x:Class="CsvToMongoDb.QueryClient.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Background="{StaticResource WindowBackground}"
        Title="ShellView"
        Height="450"
        Width="800">
    <Window.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="Foreground"
                        Value="White">
                </Setter>
            </Style>
            <Style TargetType="ListView">
                <Setter Property="Background"
                        Value="{StaticResource WindowBackground}" />
                <Setter Property="BorderBrush" Value="#36393B"></Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <DockPanel>
            <Label Content="MachineID:"
                   DockPanel.Dock="Top">
            </Label>
            <ComboBox DockPanel.Dock="Top"
                      ItemsSource="{Binding MachineIds}"
                      SelectedItem="{Binding SelectedMachineId}">
            </ComboBox>
            <Label Content="Parameters:"
                   DockPanel.Dock="Top">
            </Label>
            <ListView ItemsSource="{Binding Parameters}"
                      DockPanel.Dock="Top">
            </ListView>
        </DockPanel>
    </Grid>
</Window>